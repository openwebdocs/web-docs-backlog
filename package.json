{
  "name": "web-docs-backlog",
  "description": "Backlog for web platform docs",
  "devDependencies": {
    "@11ty/eleventy": "3.0.0",
    "@mdn/browser-compat-data": "^6.0.14",
    "compute-baseline": "^0.3.1"
  },
  "scripts": {
    "build": "npx rimraf docs && npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve"
  },
  "type": "module"
}
